#include<stdio.h>
void display(int arr[],int n)
{
    for(int i=0;i<n;i++)
    {
        printf("%d\n",arr[i]);
    }
    printf("\n");
}
int partition(int arr[],int p,int p)
{
    int x=arr[r];
    int i=p-1;
    for(int j-p;j<r;j++)
    {
        if(arr[j]<x)
        {
            i++;
            int temp=arr
        }
    }
}
